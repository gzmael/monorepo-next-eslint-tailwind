/* eslint-disable @typescript-eslint/no-var-requires */
const { theme } = require('tailwind-config/tailwind.config')

/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [],
  theme: {
    ...theme,
  },
  plugins: [],
}
